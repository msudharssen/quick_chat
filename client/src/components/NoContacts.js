import React from 'react';

const NoContacts = function(props) {

    return (

        <div className="no-contacts">

            <p>You currently have no saved contacts</p>

        </div>

    );
}

export default NoContacts;